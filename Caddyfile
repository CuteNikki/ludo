ludo.niso.moe {
    # Route WebSocket traffic to the Bun backend container
    handle /ws* {
        reverse_proxy server:3001
    }

    # Route all other web traffic to the Next.js frontend container
    handle {
        reverse_proxy web:3000
    }
}